node_modules/
# dist/ is intentionally tracked so jsDelivr (and other CDNs that
# serve raw repo files) have something to ship. Rebuild it via
# `npm run build` before tagging a release.
.env*.local
.DS_Store
*.log
coverage/
.vitest/

# stray local copies of the live SCC HTML (not project code)
index1.html

# autoconnect writes a timestamped backup of graph.json on every run
data/graph.json.bak-*
